/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Aaro API
 * OpenAPI spec version: v1
 */
import type { AaroMVCControllersWebApiStokBayiModelTipID } from './aaroMVCControllersWebApiStokBayiModelTipID';

export interface AaroMVCControllersWebApiStokBayiModel {
  /** Ürünün sistemdeki benzersiz stok ID'si. */
  StokID?: number;
  /** Ürünün stok kodu (SKU). */
  StokKodu?: string;
  /** Ürünün adı / başlığı. */
  StokAdi?: string;
  /** Ürünün teknik kalınlık ölçüsü. */
  Kalinlik?: number;
  /** Ürünün teknik en ölçüsü. */
  En?: number;
  /** Ürünün teknik boy ölçüsü. */
  Boy?: number;
  /** Ürünün net ağırlığı. */
  Agirlik?: number;
  /** Ürünün tablo tipi ID'si (Mamul, Yarı Mamul, Ticari Mal vb.). */
  TipID?: AaroMVCControllersWebApiStokBayiModelTipID;
  /** Ürünün tip adı açıklaması. */
  TipAdi?: string;
  /** Ürünün temel birim kodu (Örn: AD, KG, M). */
  Brm1Kodu?: string;
  /** Ürünün temel birim adı (Örn: Adet, Kg, Metre). */
  Brm1Adi?: string;
  /** Ürünün 1. seviye grup/kategori ID'si. */
  Kod1ID?: number;
  /** Ürünün 1. seviye grup/kategori adı. */
  Kod1Adi?: string;
  /** Ürünün 2. seviye grup/kategori ID'si. */
  Kod2ID?: number;
  /** Ürünün 2. seviye grup/kategori adı. */
  Kod2Adi?: string;
  /** Ürünün 3. seviye grup/kategori ID'si. */
  Kod3ID?: number;
  /** Ürünün 3. seviye grup/kategori adı. */
  Kod3Adi?: string;
  /** Ürünün 4. seviye grup/kategori ID'si. */
  Kod4ID?: number;
  /** Ürünün 4. seviye grup/kategori adı. */
  Kod4Adi?: string;
  /** Ürünün 5. seviye grup/kategori ID'si. */
  Kod5ID?: number;
  /** Ürünün 5. seviye grup/kategori adı. */
  Kod5Adi?: string;
  /** Ürünün 6. seviye grup/kategori ID'si. */
  Kod6ID?: number;
  /** Ürünün 6. seviye grup/kategori adı. */
  Kod6Adi?: string;
  /** Ürüne atanan 1. etiket ID'si. */
  Etiket1ID?: number;
  /** &gt;Ürüne atanan 1. etiket adı. */
  Etiket1Adi?: string;
  /** Ürüne atanan 2. etiket ID'si. */
  Etiket2ID?: number;
  /** Ürüne atanan 2. etiket adı. */
  Etiket2Adi?: string;
  /** Ürüne atanan 3. etiket ID'si. */
  Etiket3ID?: number;
  /** Ürüne atanan 3. etiket adı. */
  Etiket3Adi?: string;
  /** Ürüne atanan 4. etiket ID'si. */
  Etiket4ID?: number;
  /** Ürüne atanan 4. etiket adı. */
  Etiket4Adi?: string;
  /** Ürüne atanan 5. etiket ID'si. */
  Etiket5ID?: number;
  /** Ürüne atanan 5. etiket adı. */
  Etiket5Adi?: string;
  /** Ürünün barkod numarası. */
  Barkod?: string;
  /** Ürünün kısa açıklaması veya teknik notu. */
  Aciklama?: string;
  /** Ürünün detaylı açıklamalarının tutulduğu tabloya ait ID'si. */
  AciklamalarID?: number;
  /** Bayinin görebileceği güncel depo stok miktarı. */
  DepoMiktar?: number;
  /** Depoda tutulabilecek veya sipariş edilebilecek maksimum miktar. */
  DepoMaxMiktar?: number;
  /** Ürünün liste satış fiyatı. */
  Fiyat?: number;
  /** Bayiye özel iskontolar uygulandıktan sonraki net fiyat. */
  FiyatIskontolu?: number;
  /** Fiyatın para birimi ID'si. */
  FiyatDovizID?: number;
  /** Para birimi kısa kodu (Örn: TRY, USD). */
  FiyatDovizKodu?: string;
  /** Para birimi tam adı. */
  FiyatDovizAdi?: string;
  /** Gösterilen fiyatın Vergiye (KDV vs.) dahil olup olmadığını belirtir. */
  FiyataVergiDahilMi?: boolean;
  /** Ürüne uygulanan KDV oranı. */
  FiyatKDVOrani?: number;
  /** Ürünün görsel URL adresi. (Domain İsmi + Resim Adresi) */
  ResimAdresi?: string;
  /** Ürünün satın alınmasıyla kazanılacak sadakat puanı. */
  KartPuan?: number;
  /** Arama ve filtreleme için kullanılan birleşik kısıt metni. */
  Kisit?: string;
}
