/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Aaro API
 * OpenAPI spec version: v1
 */
import type { AaroMVCControllersWebApiBaslikModelOnayDurum } from './aaroMVCControllersWebApiBaslikModelOnayDurum';
import type { AaroMVCControllersWebApiBaslikModelEFaturaProfilID } from './aaroMVCControllersWebApiBaslikModelEFaturaProfilID';
import type { AaroMVCControllersWebApiBaslikModelEFaturaTipID } from './aaroMVCControllersWebApiBaslikModelEFaturaTipID';
import type { AaroMVCControllersWebApiBaslikModelEFaturaDurumID } from './aaroMVCControllersWebApiBaslikModelEFaturaDurumID';
import type { AaroMVCControllersWebApiBaslikModelEFaturaTurID } from './aaroMVCControllersWebApiBaslikModelEFaturaTurID';
import type { AaroMVCControllersWebApiBaslikModelTipID } from './aaroMVCControllersWebApiBaslikModelTipID';

export interface AaroMVCControllersWebApiBaslikModel {
  /** Belgenin sistemdeki benzersiz temel ID'si. */
  DekontID?: number;
  /** Belgenin resmi evrak tarihi. */
  Tarih?: Date;
  /** Kullanıcı tarafından girilen veya otomatik üretilen belge numarası. */
  BelgeNo?: string;
  /** Belgenin ilk/orijinal numarasını tutar. e-Faturalarda BelgeNo değişse bile referans takibi için kullanılır. */
  RefBelgeNo?: string;
  /** Belgenin genel ödeme vadesi. */
  Vade?: Date;
  /** Belgenin genel toplam tutarı. */
  Tutar?: number;
  /** Belgenin hiyerarşik veya derinlik seviyesi. */
  Seviye?: number;
  /** Kayıt durumu (Aktif/Pasif veya Taslak/Kesinleşmiş). */
  Durum?: boolean;
  /** Sistem içi kullanım için ayrılmış iç açıklama notu. */
  Aciklama?: string;
  /** Belgenin iş akışı üzerindeki onay durumu (Beklemede, Onaylandı, Reddedildi vb.). */
  OnayDurum?: AaroMVCControllersWebApiBaslikModelOnayDurum;
  /** Kaydın ilk oluşturulma zaman damgası. */
  OlsTar?: Date;
  /** Kaydın son değiştirilme zaman damgası. */
  DgsTar?: Date;
  /** Özel raporlama ihtiyaçları için ayrılmış numerik yedek alan 1. */
  YedekN1?: number;
  /** Özel raporlama ihtiyaçları için ayrılmış numerik yedek alan 2. */
  YedekN2?: number;
  /** Özel raporlama ihtiyaçları için ayrılmış numerik yedek alan 3. */
  YedekN3?: number;
  /** Özel raporlama ihtiyaçları için ayrılmış metin tabanlı yedek alan 1. */
  YedekC1?: string;
  /** Özel raporlama ihtiyaçları için ayrılmış metin tabanlı yedek alan 2. */
  YedekC2?: string;
  /** Özel raporlama ihtiyaçları için ayrılmış metin tabanlı yedek alan 3. */
  YedekC3?: string;
  /** Özel raporlama ihtiyaçları için ayrılmış tamsayı yedek alan 1. */
  YedekI1?: number;
  /** Özel raporlama ihtiyaçları için ayrılmış tamsayı yedek alan 2. */
  YedekI2?: number;
  /** Özel raporlama ihtiyaçları için ayrılmış tamsayı yedek alan 3. */
  YedekI3?: number;
  /** Lojistik belgeler (Sipariş/Teklif) için fiziksel teslimatın veya termin tarihinin beklendiği gün. */
  RefTeslimTarihi?: Date;
  /** Lojistik belgeler için işlem tamamlanma (Sevk edildi/İptal edildi) bayrağı. */
  Kapali?: boolean;
  /** Belgenin dijital ortamda (e-Fatura/e-Arşiv) gönderilip gönderilmeyeceğini belirtir. */
  eFatura?: boolean;
  /** e-Belge için GİB/Entegratör tarafında atanan benzersiz evrensel numara. */
  eFaturaUUID?: string;
  /** e-Belge profil tipi (Ticari, Temel vb.). */
  eFaturaProfilID?: AaroMVCControllersWebApiBaslikModelEFaturaProfilID;
  /** e-Belge gönderim tipi (Satış, İade, Tevkifat vb.). */
  eFaturaTipID?: AaroMVCControllersWebApiBaslikModelEFaturaTipID;
  /** e-Belgenin entegratör üzerindeki güncel statüsü. */
  eFaturaDurumID?: AaroMVCControllersWebApiBaslikModelEFaturaDurumID;
  /** Belgenin yasal türü (e-Fatura veya e-Arşiv ayrımı). */
  eFaturaTurID?: AaroMVCControllersWebApiBaslikModelEFaturaTurID;
  /** e-Arşiv internet satışlarında kullanılacak çıktı şablonu adı. */
  eArsivInternetSatisSablon?: string;
  /** Entegrasyon kanallarından (Amazon, Trendyol vb.) gelen mağaza sipariş numarası (ID'si). */
  MagazaSiparisID?: number;
  /** Belgeye atanan 1. seviye takip etiketi ID'si. */
  Etiket1ID?: number;
  /** Belgeye atanan 2. seviye takip etiketi ID'si. */
  Etiket2ID?: number;
  /** Belgeye atanan 3. seviye takip etiketi ID'si. */
  Etiket3ID?: number;
  /** Belgeye atanan 4. seviye takip etiketi ID'si. */
  Etiket4ID?: number;
  /** Belgeye atanan 5. seviye takip etiketi ID'si. */
  Etiket5ID?: number;
  /** Kopyalanarak oluşturulan belgelerde kaynak belgenin ID'si. */
  KopyaKaynakID?: number;
  /** İşlemin yapıldığı aktif şube ID'si. */
  SubeID?: number;
  /** İşlemin yapıldığı aktif şirket ID'si. */
  SirketID?: number;
  /** Şubeler arası virman veya transfer işlemlerinde hedef/karşı şube ID'si. */
  Sube2ID?: number;
  /** Belgenin genel hareket türü (SatisFaturasi, AlinanSiparis vb. - HareketTurleriEnum). */
  TipID?: AaroMVCControllersWebApiBaslikModelTipID;
  /** Belge türünün alt kategorisi (Detaylı tip ayrımı). */
  TipAltID?: number;
  /** Belgeye ait genişletilmiş notlar veya açıklama metinlerinin tutulduğu tablo ID'si. */
  AciklamalarID?: number;
  /** Kaydı oluşturan kullanıcının ID'si. */
  OlsID?: number;
  /** Kaydı son güncelleyen kullanıcının ID'si. */
  DgsID?: number;
  /** Belgenin bağlı olduğu aktif sözleşme ID'si. */
  RefSozlesmeID?: number;
  /** Belge geneli için belirlenen varsayılan depo ID'si. */
  RefDepoID?: number;
  /** Belge toplamının varsayılan olarak KDV dahil hesaplanıp hesaplanmayacağı. */
  RefKDVDahil?: boolean;
  /** Belgenin maliyet ve kar takibi için bağlı olduğu proje ID'si. */
  RefProjeID?: number;
  /** Belgeden sorumlu olan satış temsilcisi (plasiyer) ID'si. */
  RefPlasiyerID?: number;
  /** Depolar arası transferlerde hedef depo referansı. */
  RefDepo2ID?: number;
  /** Dış ticaret işlemlerinde kullanılan İthalat/İhracat dosyası referansı. */
  RefIthalatIhracatID?: number;
  /** Tekliflerin yasal veya ticari son geçerlilik tarihi. */
  GecerlilikTarihi?: Date;
  /** Belgenin tarafındaki ana cari hesabın (Müşteri/Tedarikçi) unvanı. */
  KartAdi?: string;
  /** Tek taraflı (single-leg) hareketlerde kullanılan döviz birimi ID'si. */
  KalemTekDovizID?: number;
  /** Çok taraflı (multi-leg) hareketlerde kullanılan döviz birimi ID'si. */
  KalemCokDovizID?: number;
  /** Tek taraflı hareketler için fiksasyon kuru. */
  KalemTekDovizKuru?: number;
  /** Çok taraflı hareketler için fiksasyon kuru. */
  KalemCokDovizKuru?: number;
}
