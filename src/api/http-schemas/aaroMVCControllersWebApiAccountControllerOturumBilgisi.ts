/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Aaro API
 * OpenAPI spec version: v1
 */

export interface AaroMVCControllersWebApiAccountControllerOturumBilgisi {
  /** Kullanıcının bağlı olduğu ana grubun ID'si). */
  GrupID?: number;
  /** Kullanıcının bağlı olduğu ana grubun adı (Genellikle şirketler grubu). */
  GrupAdi?: string;
  /** Aktif çalışılan şirket ID'si. */
  SirketID?: number;
  /** Aktif çalışılan şirketin tam ticari ünvanı. */
  SirketAdi?: string;
  /** Aktif çalışılan şube ID'si. */
  SubeID?: number;
  /** Aktif çalışılan şubenin adı. */
  SubeAdi?: string;
  /** Kullanıcının yetki grubunun sistem ID'si. */
  KullaniciGrupID?: number;
  /** Kullanıcı grubunun kısa kodu (Örn: ADM, SAT, MUH). */
  KullaniciGrupKodu?: string;
  /** Kullanıcı grubunun tam adı (Örn: Sistem Yöneticisi). */
  KullaniciGrupAdi?: string;
  /** Kullanıcının sistemdeki benzersiz sayısal ID'si. */
  KullaniciKisaID?: number;
  /** Kullanıcının giriş yaptığı kullanıcı kodu/adı. */
  KullaniciKodu?: string;
  /** Kullanıcının sistemde tanımlı kullanıcı adı. */
  KullaniciAdi?: string;
  /** Kullanıcının geçmişe dönük işlem yapabileceği en eski tarih sınırı. */
  KayitTarihBaslangic?: string;
  /** Kullanıcının geleceğe dönük işlem yapabileceği en son tarih sınırı. */
  KayitTarihBitis?: string;
  /** Aktif oturumun sistem tarafından başlatıldığı tarih ve saat. */
  OturumBaslangic?: string;
  /** Sisteme her istekte gönderilmesi gereken benzersiz oturum (GUID) anahtarı. */
  OturumID?: string;
  /** Kullanıcının oturum açabileceği günlük minimum saat sınırı. */
  OtrumBaslangicSaatiMin?: string;
  /** Kullanıcının oturum açabileceği günlük maksimum saat sınırı. */
  OtrumBitisSaatiMax?: string;
}
